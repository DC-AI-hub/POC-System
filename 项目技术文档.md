# 港交所POC系统 - 技术方案文档

## 1. 技术架构概览

### 1.1 系统架构
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端应用层     │    │   API网关层       │    │   微服务层       │
│   Next.js       │◄──►│   nginx/Kong    │◄──►│   Node.js       │
│   React         │    │   身份认证       │    │   Express       │
│   TypeScript    │    │   负载均衡       │    │   工作流引擎     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                       ▲
                                                       │
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   缓存层         │    │   数据持久层      │    │   消息队列       │
│   Redis         │    │   PostgreSQL    │    │   RabbitMQ      │
│   Session Store │    │   MongoDB       │    │   事件驱动       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 1.2 技术栈选型

**前端技术栈**：
- **框架**: Next.js 15.2.4 (React 19)
- **语言**: TypeScript 5+
- **样式**: Tailwind CSS + Radix UI
- **状态管理**: React Hook Form + Zod
- **构建工具**: Next.js内置构建系统
- **包管理**: pnpm

**后端技术栈**：
- **运行时**: Node.js 18+
- **框架**: Express.js / Fastify
- **语言**: TypeScript
- **数据库**: PostgreSQL (主库) + Redis (缓存)
- **ORM**: Prisma / TypeORM
- **工作流引擎**: Temporal.io / Zeebe
- **API文档**: OpenAPI/Swagger

**基础设施**：
- **容器化**: Docker + Docker Compose
- **网关**: nginx / Kong
- **监控**: Prometheus + Grafana
- **日志**: ELK Stack (Elasticsearch, Logstash, Kibana)

## 2. 前端架构设计

### 2.1 项目结构
```
fronted/
├── app/                    # Next.js App Router
│   ├── globals.css        # 全局样式
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 主页面
├── components/            # React组件
│   ├── ui/                # 基础UI组件
│   ├── forms/             # 表单组件
│   ├── tables/            # 表格组件
│   └── workflow/          # 工作流组件
├── lib/                   # 工具库
│   ├── api.ts            # API客户端
│   ├── auth.ts           # 认证逻辑
│   ├── utils.ts          # 工具函数
│   └── validations.ts    # 表单验证
├── hooks/                 # 自定义Hooks
├── types/                 # TypeScript类型定义
├── constants/             # 常量定义
└── stores/               # 状态管理
```

### 2.2 组件设计模式

**原子化设计**：
- **Atoms**: Button, Input, Label等基础组件
- **Molecules**: SearchBox, FormField等复合组件
- **Organisms**: DataTable, ExpenseForm等复杂组件
- **Templates**: PageLayout, FormLayout等布局模板
- **Pages**: ExpenseApplicationPage等页面组件

**组件设计原则**：
```typescript
// 组件props接口设计
interface BaseComponentProps {
  className?: string;
  children?: React.ReactNode;
  'data-testid'?: string;
}

// 表单组件设计
interface FormFieldProps extends BaseComponentProps {
  label: string;
  error?: string;
  required?: boolean;
  disabled?: boolean;
}

// 数据表格组件设计
interface DataTableProps<T> extends BaseComponentProps {
  data: T[];
  columns: ColumnDef<T>[];
  loading?: boolean;
  pagination?: PaginationConfig;
  onRowSelect?: (row: T) => void;
}
```

### 2.3 状态管理架构

**状态分类**：
- **本地状态**: useState, useReducer
- **服务端状态**: TanStack Query (React Query)
- **表单状态**: React Hook Form
- **全局状态**: Zustand (轻量级状态管理)

```typescript
// API状态管理
export const useExpenseApplications = () => {
  return useQuery({
    queryKey: ['expense-applications'],
    queryFn: api.getExpenseApplications,
    staleTime: 5 * 60 * 1000, // 5分钟
  });
};

// 全局状态管理
interface AuthStore {
  user: User | null;
  token: string | null;
  login: (credentials: LoginCredentials) => Promise<void>;
  logout: () => void;
}

export const useAuthStore = create<AuthStore>((set) => ({
  user: null,
  token: null,
  login: async (credentials) => {
    const response = await api.login(credentials);
    set({ user: response.user, token: response.token });
  },
  logout: () => set({ user: null, token: null }),
}));
```

## 3. 后端架构设计

### 3.1 微服务架构

**服务划分**：
- **用户服务** (User Service): 用户认证、权限管理
- **费用服务** (Expense Service): 费用申请、报销管理
- **工作流服务** (Workflow Service): 审批流程引擎
- **通知服务** (Notification Service): 消息推送、邮件通知
- **文件服务** (File Service): 文件上传、存储管理

### 3.2 数据库设计

**主要实体关系**：
```sql
-- 用户表
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_id VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    department_id UUID REFERENCES departments(id),
    role VARCHAR(50) NOT NULL,
    status VARCHAR(20) DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 费用申请表
CREATE TABLE expense_applications (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    application_number VARCHAR(50) UNIQUE NOT NULL,
    applicant_id UUID REFERENCES users(id),
    department_id UUID REFERENCES departments(id),
    company VARCHAR(100),
    apply_date DATE NOT NULL,
    expense_date DATE NOT NULL,
    description TEXT,
    total_amount DECIMAL(15,2) DEFAULT 0,
    status VARCHAR(20) DEFAULT 'draft',
    workflow_instance_id UUID,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 费用明细表
CREATE TABLE expense_items (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    application_id UUID REFERENCES expense_applications(id),
    category VARCHAR(100) NOT NULL,
    purpose TEXT,
    amount DECIMAL(15,2) NOT NULL,
    sort_order INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 工作流实例表
CREATE TABLE workflow_instances (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    workflow_definition_id UUID,
    entity_type VARCHAR(50) NOT NULL,
    entity_id UUID NOT NULL,
    status VARCHAR(20) DEFAULT 'running',
    current_step VARCHAR(100),
    started_by UUID REFERENCES users(id),
    started_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP
);

-- 审批记录表  
CREATE TABLE approval_records (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    workflow_instance_id UUID REFERENCES workflow_instances(id),
    step_name VARCHAR(100) NOT NULL,
    approver_id UUID REFERENCES users(id),
    action VARCHAR(20) NOT NULL, -- approve, reject, delegate
    comment TEXT,
    approved_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 3.3 API设计规范

**RESTful API设计**：
```typescript
// API路由设计
/api/v1/auth
  POST /login                 # 用户登录
  POST /logout                # 用户登出
  GET  /profile               # 获取用户信息

/api/v1/expense-applications
  GET    /                    # 获取费用申请列表
  POST   /                    # 创建费用申请
  GET    /:id                 # 获取费用申请详情
  PUT    /:id                 # 更新费用申请
  DELETE /:id                 # 删除费用申请
  POST   /:id/submit          # 提交审批
  GET    /:id/history         # 获取审批历史

/api/v1/workflow
  GET    /definitions         # 获取工作流定义
  POST   /instances           # 创建工作流实例
  PUT    /instances/:id/approve  # 审批通过
  PUT    /instances/:id/reject   # 审批拒绝
  GET    /instances/:id/tasks    # 获取待办任务

// API响应格式
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  message?: string;
  errors?: string[];
  pagination?: {
    page: number;
    pageSize: number;
    total: number;
  };
}
```

### 3.4 工作流引擎设计

**工作流定义**：
```typescript
interface WorkflowDefinition {
  id: string;
  name: string;
  version: string;
  nodes: WorkflowNode[];
  edges: WorkflowEdge[];
  conditions: WorkflowCondition[];
}

interface WorkflowNode {
  id: string;
  type: 'start' | 'task' | 'gateway' | 'end';
  name: string;
  assignee?: string;
  candidateGroups?: string[];
  formKey?: string;
  properties?: Record<string, any>;
}

// 工作流执行引擎
class WorkflowEngine {
  async startProcess(definitionId: string, variables: any): Promise<string> {
    // 创建工作流实例
    const instance = await this.createInstance(definitionId, variables);
    
    // 执行第一个节点
    await this.executeNode(instance.currentNode, variables);
    
    return instance.id;
  }

  async completeTask(taskId: string, variables: any): Promise<void> {
    const task = await this.getTask(taskId);
    const instance = await this.getInstance(task.instanceId);
    
    // 完成当前任务
    await this.completeNode(task.nodeId, variables);
    
    // 移动到下一个节点
    const nextNode = this.getNextNode(instance, task.nodeId);
    if (nextNode) {
      await this.executeNode(nextNode, variables);
    } else {
      await this.completeInstance(instance.id);
    }
  }
}
```

## 4. 技术实现细节

### 4.1 认证与授权

**JWT认证机制**：
```typescript
// JWT令牌结构
interface JWTPayload {
  userId: string;
  employeeId: string;
  email: string;
  roles: string[];
  permissions: string[];
  iat: number;
  exp: number;
}

// 权限控制中间件
export const requireAuth = (roles?: string[]) => {
  return async (req: Request, res: Response, next: NextFunction) => {
    try {
      const token = req.headers.authorization?.replace('Bearer ', '');
      if (!token) {
        return res.status(401).json({ message: '未授权访问' });
      }

      const payload = jwt.verify(token, JWT_SECRET) as JWTPayload;
      
      if (roles && !roles.some(role => payload.roles.includes(role))) {
        return res.status(403).json({ message: '权限不足' });
      }

      req.user = payload;
      next();
    } catch (error) {
      return res.status(401).json({ message: '令牌无效' });
    }
  };
};
```

### 4.2 数据验证

**输入验证**：
```typescript
// Zod验证模式
export const ExpenseApplicationSchema = z.object({
  applicantId: z.string().uuid(),
  departmentId: z.string().uuid(),
  company: z.string().min(1, '公司名称不能为空'),
  applyDate: z.coerce.date(),
  expenseDate: z.coerce.date(),
  description: z.string().min(1, '事由描述不能为空'),
  items: z.array(z.object({
    category: z.string().min(1, '费用科目不能为空'),
    purpose: z.string().min(1, '费用用途不能为空'),
    amount: z.number().positive('金额必须大于0'),
  })).min(1, '至少需要一条费用明细'),
});

// 验证中间件
export const validateBody = (schema: z.ZodSchema) => {
  return (req: Request, res: Response, next: NextFunction) => {
    try {
      schema.parse(req.body);
      next();
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({
          message: '数据验证失败',
          errors: error.errors.map(e => `${e.path.join('.')}: ${e.message}`)
        });
      }
      next(error);
    }
  };
};
```

### 4.3 错误处理

**统一错误处理**：
```typescript
// 自定义错误类
export class AppError extends Error {
  constructor(
    public message: string,
    public statusCode: number = 500,
    public code?: string
  ) {
    super(message);
    this.name = 'AppError';
  }
}

// 全局错误处理中间件
export const errorHandler = (
  err: Error,
  req: Request,
  res: Response,
  next: NextFunction
) => {
  logger.error(err);

  if (err instanceof AppError) {
    return res.status(err.statusCode).json({
      success: false,
      message: err.message,
      code: err.code,
    });
  }

  if (err instanceof z.ZodError) {
    return res.status(400).json({
      success: false,
      message: '数据验证失败',
      errors: err.errors,
    });
  }

  res.status(500).json({
    success: false,
    message: '服务器内部错误',
  });
};
```

### 4.4 缓存策略

**多级缓存**：
```typescript
// Redis缓存封装
class CacheService {
  constructor(private redis: Redis) {}

  async get<T>(key: string): Promise<T | null> {
    const value = await this.redis.get(key);
    return value ? JSON.parse(value) : null;
  }

  async set(key: string, value: any, ttl: number = 3600): Promise<void> {
    await this.redis.setex(key, ttl, JSON.stringify(value));
  }

  async del(key: string): Promise<void> {
    await this.redis.del(key);
  }

  async invalidatePattern(pattern: string): Promise<void> {
    const keys = await this.redis.keys(pattern);
    if (keys.length > 0) {
      await this.redis.del(...keys);
    }
  }
}

// 缓存装饰器
export const Cacheable = (ttl: number = 3600) => {
  return (target: any, propertyKey: string, descriptor: PropertyDescriptor) => {
    const originalMethod = descriptor.value;
    
    descriptor.value = async function (...args: any[]) {
      const cacheKey = `${target.constructor.name}:${propertyKey}:${JSON.stringify(args)}`;
      
      let result = await cacheService.get(cacheKey);
      if (!result) {
        result = await originalMethod.apply(this, args);
        await cacheService.set(cacheKey, result, ttl);
      }
      
      return result;
    };
  };
};
```

## 5. 性能优化方案

### 5.1 前端性能优化

**代码分割与懒加载**：
```typescript
// 动态导入组件
const ExpenseApplicationPage = lazy(() => 
  import('./components/expense-application-page')
);

// 路由级别代码分割
const DashboardPage = lazy(() => import('./pages/dashboard'));
const ReportsPage = lazy(() => import('./pages/reports'));

// 组件懒加载包装器
const LazyWrapper: React.FC<{ children: React.ReactNode }> = ({ children }) => (
  <Suspense fallback={<LoadingSpinner />}>
    {children}
  </Suspense>
);
```

**图片和资源优化**：
```typescript
// Next.js Image组件优化
import Image from 'next/image'

export const OptimizedImage: React.FC<ImageProps> = ({ src, alt, ...props }) => (
  <Image
    src={src}
    alt={alt}
    loading="lazy"
    placeholder="blur"
    blurDataURL="data:image/jpeg;base64,..."
    sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
    {...props}
  />
);
```

### 5.2 后端性能优化

**数据库查询优化**：
```sql
-- 索引设计
CREATE INDEX idx_expense_applications_applicant_date 
ON expense_applications(applicant_id, apply_date DESC);

CREATE INDEX idx_workflow_instances_entity 
ON workflow_instances(entity_type, entity_id);

CREATE INDEX idx_approval_records_workflow_step 
ON approval_records(workflow_instance_id, step_name);

-- 查询优化
-- 分页查询
SELECT * FROM expense_applications 
WHERE applicant_id = $1 
ORDER BY apply_date DESC 
LIMIT $2 OFFSET $3;

-- 连接查询优化
SELECT ea.*, u.name as applicant_name, d.name as department_name
FROM expense_applications ea
JOIN users u ON ea.applicant_id = u.id
JOIN departments d ON ea.department_id = d.id
WHERE ea.status = 'pending'
ORDER BY ea.apply_date DESC;
```

**API响应优化**：
```typescript
// 分页响应
export const paginate = async <T>(
  query: any,
  page: number = 1,
  pageSize: number = 20
): Promise<PaginatedResult<T>> => {
  const offset = (page - 1) * pageSize;
  
  const [data, total] = await Promise.all([
    query.offset(offset).limit(pageSize),
    query.count()
  ]);

  return {
    data,
    pagination: {
      page,
      pageSize,
      total,
      totalPages: Math.ceil(total / pageSize),
      hasNext: page * pageSize < total,
      hasPrev: page > 1
    }
  };
};

// 响应压缩
app.use(compression({
  level: 6,
  threshold: 1024,
}));
```

## 6. 安全实施方案

### 6.1 数据安全

**敏感数据加密**：
```typescript
// 数据加密服务
class EncryptionService {
  private readonly key: Buffer;
  
  constructor(secretKey: string) {
    this.key = crypto.scryptSync(secretKey, 'salt', 32);
  }

  encrypt(text: string): string {
    const iv = crypto.randomBytes(16);
    const cipher = crypto.createCipher('aes-256-gcm', this.key);
    cipher.setIV(iv);
    
    let encrypted = cipher.update(text, 'utf8', 'hex');
    encrypted += cipher.final('hex');
    
    const authTag = cipher.getAuthTag();
    
    return `${iv.toString('hex')}:${authTag.toString('hex')}:${encrypted}`;
  }

  decrypt(encryptedText: string): string {
    const [ivHex, authTagHex, encrypted] = encryptedText.split(':');
    
    const iv = Buffer.from(ivHex, 'hex');
    const authTag = Buffer.from(authTagHex, 'hex');
    
    const decipher = crypto.createDecipher('aes-256-gcm', this.key);
    decipher.setIV(iv);
    decipher.setAuthTag(authTag);
    
    let decrypted = decipher.update(encrypted, 'hex', 'utf8');
    decrypted += decipher.final('utf8');
    
    return decrypted;
  }
}
```

### 6.2 API安全

**请求限流**：
```typescript
// 限流中间件
import rateLimit from 'express-rate-limit';

export const createRateLimit = (windowMs: number, max: number) => {
  return rateLimit({
    windowMs,
    max,
    message: {
      error: '请求频率过高，请稍后再试',
      retryAfter: Math.ceil(windowMs / 1000)
    },
    standardHeaders: true,
    legacyHeaders: false,
  });
};

// 不同端点的限流策略
app.use('/api/auth/login', createRateLimit(15 * 60 * 1000, 5)); // 15分钟5次
app.use('/api/', createRateLimit(15 * 60 * 1000, 100)); // 15分钟100次
```

**SQL注入防护**：
```typescript
// 参数化查询
export const expenseRepository = {
  async findById(id: string): Promise<ExpenseApplication | null> {
    const result = await db.query(
      'SELECT * FROM expense_applications WHERE id = $1',
      [id]
    );
    return result.rows[0] || null;
  },

  async findByApplicant(applicantId: string, filters: any): Promise<ExpenseApplication[]> {
    let query = 'SELECT * FROM expense_applications WHERE applicant_id = $1';
    const params = [applicantId];
    
    if (filters.status) {
      query += ' AND status = $2';
      params.push(filters.status);
    }
    
    if (filters.dateFrom) {
      query += ` AND apply_date >= $${params.length + 1}`;
      params.push(filters.dateFrom);
    }
    
    const result = await db.query(query, params);
    return result.rows;
  }
};
```

## 7. 测试策略

### 7.1 前端测试

**单元测试**：
```typescript
// Jest + React Testing Library
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { ExpenseApplicationForm } from './expense-application-form';

describe('ExpenseApplicationForm', () => {
  it('should submit form with valid data', async () => {
    const onSubmit = jest.fn();
    
    render(<ExpenseApplicationForm onSubmit={onSubmit} />);
    
    // 填写表单
    fireEvent.change(screen.getByLabelText('申请人'), {
      target: { value: 'John Doe' }
    });
    
    fireEvent.change(screen.getByLabelText('事由描述'), {
      target: { value: '办公用品采购' }
    });
    
    // 提交表单
    fireEvent.click(screen.getByText('提交审批'));
    
    await waitFor(() => {
      expect(onSubmit).toHaveBeenCalledWith({
        applicant: 'John Doe',
        description: '办公用品采购',
        // ... 其他字段
      });
    });
  });

  it('should show validation errors for invalid data', async () => {
    render(<ExpenseApplicationForm onSubmit={jest.fn()} />);
    
    fireEvent.click(screen.getByText('提交审批'));
    
    await waitFor(() => {
      expect(screen.getByText('申请人不能为空')).toBeInTheDocument();
      expect(screen.getByText('事由描述不能为空')).toBeInTheDocument();
    });
  });
});
```

**端到端测试**：
```typescript
// Playwright E2E测试
import { test, expect } from '@playwright/test';

test.describe('费用申请流程', () => {
  test('完整的费用申请和审批流程', async ({ page }) => {
    // 登录
    await page.goto('/login');
    await page.fill('input[name="email"]', 'test@company.com');
    await page.fill('input[name="password"]', 'password123');
    await page.click('button[type="submit"]');
    
    // 导航到费用申请页面
    await page.click('text=日常费用申请');
    
    // 填写申请表单
    await page.fill('input[name="description"]', '办公用品采购');
    await page.selectOption('select[name="company"]', 'CHC');
    
    // 添加费用明细
    await page.click('text=增加明细');
    await page.fill('input[name="items.0.category"]', '办公用品');
    await page.fill('input[name="items.0.purpose"]', '购买文具');
    await page.fill('input[name="items.0.amount"]', '500');
    
    // 提交申请
    await page.click('text=提交审批');
    
    // 验证提交成功
    await expect(page.locator('text=申请提交成功')).toBeVisible();
    
    // 验证状态更新
    await expect(page.locator('text=审批中')).toBeVisible();
  });
});
```

### 7.2 后端测试

**API测试**：
```typescript
// Jest + Supertest
import request from 'supertest';
import { app } from '../app';
import { db } from '../db';

describe('POST /api/expense-applications', () => {
  beforeEach(async () => {
    await db.clear();
    await db.seed();
  });

  it('should create new expense application', async () => {
    const token = await getAuthToken();
    
    const response = await request(app)
      .post('/api/expense-applications')
      .set('Authorization', `Bearer ${token}`)
      .send({
        company: 'CHC',
        applyDate: '2024-01-15',
        expenseDate: '2024-01-14',
        description: '办公用品采购',
        items: [
          {
            category: '办公用品',
            purpose: '购买文具',
            amount: 500
          }
        ]
      })
      .expect(201);

    expect(response.body).toMatchObject({
      success: true,
      data: {
        id: expect.any(String),
        applicationNumber: expect.any(String),
        status: 'draft',
        totalAmount: 500
      }
    });
  });

  it('should validate required fields', async () => {
    const token = await getAuthToken();
    
    const response = await request(app)
      .post('/api/expense-applications')
      .set('Authorization', `Bearer ${token}`)
      .send({})
      .expect(400);

    expect(response.body.errors).toContain('公司名称不能为空');
    expect(response.body.errors).toContain('事由描述不能为空');
  });
});
```

## 8. 部署方案

### 8.1 容器化部署

**Docker配置**：
```dockerfile
# Dockerfile.frontend
FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

FROM node:18-alpine AS runner
WORKDIR /app

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static

USER nextjs

EXPOSE 3000
ENV PORT 3000

CMD ["node", "server.js"]
```

```dockerfile
# Dockerfile.backend
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

EXPOSE 3001

CMD ["npm", "start"]
```

**Docker Compose配置**：
```yaml
version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:3001
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://user:password@postgres:5432/expense_db
      - REDIS_URL=redis://redis:6379
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=expense_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/ssl/certs
    depends_on:
      - frontend
      - backend

volumes:
  postgres_data:
  redis_data:
```

### 8.2 CI/CD流水线

**GitHub Actions配置**：
```yaml
name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
      
      - name: Run E2E tests
        run: npm run test:e2e

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Build Docker images
        run: |
          docker build -t expense-frontend ./frontend
          docker build -t expense-backend ./backend
      
      - name: Push to registry
        run: |
          docker tag expense-frontend ${{ secrets.REGISTRY_URL }}/expense-frontend:${{ github.sha }}
          docker tag expense-backend ${{ secrets.REGISTRY_URL }}/expense-backend:${{ github.sha }}
          docker push ${{ secrets.REGISTRY_URL }}/expense-frontend:${{ github.sha }}
          docker push ${{ secrets.REGISTRY_URL }}/expense-backend:${{ github.sha }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} '
            cd /opt/expense-app &&
            docker-compose pull &&
            docker-compose up -d --no-deps frontend backend
          '
```

## 9. 监控与维护

### 9.1 应用监控

**健康检查**：
```typescript
// 健康检查端点
app.get('/health', async (req, res) => {
  const health = {
    status: 'ok',
    timestamp: new Date().toISOString(),
    services: {
      database: 'ok',
      redis: 'ok',
      workflow: 'ok'
    }
  };

  try {
    // 检查数据库连接
    await db.query('SELECT 1');
  } catch (error) {
    health.services.database = 'error';
    health.status = 'error';
  }

  try {
    // 检查Redis连接
    await redis.ping();
  } catch (error) {
    health.services.redis = 'error';
    health.status = 'error';
  }

  const statusCode = health.status === 'ok' ? 200 : 500;
  res.status(statusCode).json(health);
});
```

**性能监控**：
```typescript
// Prometheus指标收集
import promClient from 'prom-client';

const httpRequestDuration = new promClient.Histogram({
  name: 'http_request_duration_seconds',
  help: 'Duration of HTTP requests in seconds',
  labelNames: ['method', 'route', 'status_code']
});

const httpRequestsTotal = new promClient.Counter({
  name: 'http_requests_total',
  help: 'Total number of HTTP requests',
  labelNames: ['method', 'route', 'status_code']
});

// 中间件
export const metricsMiddleware = (req: Request, res: Response, next: NextFunction) => {
  const start = Date.now();
  
  res.on('finish', () => {
    const duration = (Date.now() - start) / 1000;
    const labels = {
      method: req.method,
      route: req.route?.path || req.path,
      status_code: res.statusCode.toString()
    };
    
    httpRequestDuration.observe(labels, duration);
    httpRequestsTotal.inc(labels);
  });
  
  next();
};
```

### 9.2 日志管理

**结构化日志**：
```typescript
import winston from 'winston';

const logger = winston.createLogger({
  level: process.env.LOG_LEVEL || 'info',
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.errors({ stack: true }),
    winston.format.json()
  ),
  defaultMeta: {
    service: 'expense-service',
    version: process.env.APP_VERSION || '1.0.0'
  },
  transports: [
    new winston.transports.Console(),
    new winston.transports.File({ filename: 'logs/error.log', level: 'error' }),
    new winston.transports.File({ filename: 'logs/combined.log' })
  ]
});

// 使用示例
logger.info('Expense application created', {
  applicationId: 'app-123',
  applicantId: 'user-456',
  amount: 1000,
  category: 'office-supplies'
});

logger.error('Database connection failed', {
  error: error.message,
  stack: error.stack,
  query: 'SELECT * FROM users'
});
```

## 10. 总结

本技术方案文档详细描述了港交所POC系统的技术架构、实现方案和部署策略。系统采用现代化的技术栈，具备高可扩展性、高可靠性和高安全性。通过分层架构设计、微服务拆分、容器化部署等技术手段，确保系统能够满足企业级应用的各项要求。

关键技术特点：
- **前端**: Next.js + React + TypeScript + Tailwind CSS
- **后端**: Node.js + Express + PostgreSQL + Redis  
- **工作流**: 自研工作流引擎，支持复杂审批流程
- **部署**: Docker容器化 + CI/CD自动化流水线
- **监控**: Prometheus + Grafana + ELK Stack

该方案为港交所费用管理系统提供了完整的技术解决方案，支持快速开发、灵活扩展和稳定运行。 