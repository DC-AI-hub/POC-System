# Poc System 测试数据准备说明

## 登录测试数据

### 正确的登录请求格式

```json
{
    "email": "admin@hkex.com",
    "password": "admin123",
    "rememberMe": false
}
```

### 登录响应格式

```json
{
    "code": 200,
    "message": "登录成功",
    "data": {
        "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "tokenType": "Bearer",
        "expiresIn": 10800,
        "userInfo": {
            "id": 1,
            "email": "admin@hkex.com",
            "userName": "admin",
            "fullName": "系统管理员",
            "department": "IT部",
            "position": "管理员"
        }
    },
    "timestamp": 1751417754661
}
```

### 重要说明

- **访问令牌字段名**：`data.token`（不是accessToken）
- **刷新令牌字段名**：`data.refreshToken`
- **用户信息字段名**：`data.userInfo`
- **令牌类型**：`data.tokenType`（固定为"Bearer"）
- **过期时间**：`data.expiresIn`（秒）

### 刷新Token使用说明

刷新Token接口使用POST方法，参数通过表单传递：

**请求格式**：
```
POST /api/auth/refresh
Content-Type: application/x-www-form-urlencoded

refreshToken=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**响应格式**：
```json
{
    "code": 200,
    "message": "Token刷新成功",
    "data": {
        "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "expiresIn": 10800
    },
    "timestamp": 1751420633319
}
```

**注意事项**：
- 刷新Token接口不需要Authorization头
- 使用表单参数传递refreshToken
- 刷新成功后返回新的访问令牌

### 字段说明

| 字段名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| `email` | String | 是 | 用户邮箱地址 | "admin@hkex.com" |
| `password` | String | 是 | 用户密码 | "admin123" |
| `rememberMe` | Boolean | 否 | 记住登录状态 | false |

### 常见错误

1. **使用username字段** ❌
   ```json
   {
       "username": "admin",
       "password": "admin123"
   }
   ```

2. **缺少email字段** ❌
   ```json
   {
       "password": "admin123"
   }
   ```

3. **邮箱格式错误** ❌
   ```json
   {
       "email": "admin",
       "password": "admin123"
   }
   ```

## 测试用户数据

### 默认管理员账户

```json
{
    "email": "admin@hkex.com",
    "password": "admin123"
}
```

### 测试用户账户

```json
{
    "email": "testuser@hkex.com",
    "password": "test123"
}
```

## 数据库初始化

如果数据库中还没有测试用户，需要先创建用户数据：

### 方法1：通过数据库直接插入

```sql
INSERT INTO users (
    email, 
    password, 
    user_name, 
    full_name, 
    department, 
    position, 
    phone, 
    status, 
    is_online, 
    created_time
) VALUES (
    'admin@hkex.com',
    '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVEFDa', -- admin123的BCrypt加密
    'admin',
    '系统管理员',
    'IT部',
    '管理员',
    '13800138000',
    'ACTIVE',
    false,
    NOW()
);
```

### 方法2：通过用户管理接口创建

使用用户管理模块的创建用户接口：

```json
POST /api/users
{
    "email": "admin@hkex.com",
    "password": "admin123",
    "userName": "admin",
    "fullName": "系统管理员",
    "department": "IT部",
    "position": "管理员",
    "phone": "13800138000",
    "status": "ACTIVE"
}
```

## 密码加密

系统使用BCrypt加密密码，测试时可以使用以下加密后的密码：

- `admin123` → `$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVEFDa`
- `test123` → `$2a$10$8K1p/a0dL1LXMIgoEDFrwOeAQGQZQZQZQZQZQZQZQZQZQZQZQZQZ`

## 测试步骤

1. **确保数据库连接正常**
2. **创建测试用户数据**
3. **启动Spring Boot应用**
4. **使用Postman测试登录接口**
5. **验证返回的token**

## 常见问题解决

### 1. 邮箱不存在错误

**错误信息**：`用户不存在或状态异常，无法登录`

**解决方法**：
- 检查数据库中是否存在该邮箱的用户
- 确认用户状态为ACTIVE
- 使用正确的邮箱地址

### 2. 密码错误

**错误信息**：`邮箱或密码错误`

**解决方法**：
- 确认密码是否正确
- 检查密码是否已正确加密
- 使用正确的密码

### 3. 邮箱格式错误

**错误信息**：`邮箱格式不正确`

**解决方法**：
- 使用有效的邮箱格式
- 确保包含@符号和域名

## 测试环境建议

1. **使用测试数据库**：避免影响生产数据
2. **准备测试数据**：预先创建测试用户
3. **记录测试结果**：保存成功的测试用例
4. **清理测试数据**：测试完成后清理临时数据 