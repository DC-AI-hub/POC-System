# Poc System 后端需求文档

## 1. 工作流与节点管理
- 每个工作节点的状态需持久化保存到数据库。
- 支持多组工作菜单，满足不同流程配置需求。
- 工作节点支持“打回”操作，且节点状态变更需实时存库。

## 2. 报销单格式管理
- 系统需支持不同公司对应不同的报销单格式。
- 报销单格式可筛选切换，满足多公司业务场景需求。

## 3. 用户与登录管理
- 用户登录信息需存储于数据库。
- 支持在线用户实时显示（包括姓名、部门、岗位，岗位区分主管&员工）。
- 登录用户数据表需支持查询、统计等功能。

## 4. 组织架构访问
- 后端需提供组织架构数据接口，供前端UI生成组织架构图。
- 组织架构支持部门、岗位等层级关系。

## 5. 代理人设置（AB岗管理）
- 支持设置代理人（AB岗），如人员休假可转移权限。
- 代理设置需包括：被代理人、代理人、代理内容、开始时间、结束时间。
- 代理人设置可导出/编译为文档，便于管理和留档。

## 6. 日常费用申请单管理
- 支持费用申请单的填写、上传附件（如发票、证明材料等）。
- 主管审批环节支持新增附件补充。
- 申请单需包括：费用科目名称、费用明细、申请金额等字段。

## 7. 表单定制管理
- 后端需提供表单定制能力，前端可根据页面表单直接对应数据库表结构动态生成和调整表单。
- 支持表单字段的动态增减和绑定数据库表。

## 8. 日志管理
- 后台需记录系统日志、信息安全日志、审计日志。
    - 系统日志：系统运行、异常等。
    - 信息安全日志：登录、登出、权限变更，支持前端查询。
    - 审计日志：流程流转、操作时间、操作人等，支持前端查询。

---

## 技术与架构要求

- **后端框架**：Spring Boot
- **构建管理**：Maven（需配置Java环境，安装JDK1.8）
- **数据库**：PostgreSQL（Pg，支持本地开发环境部署）
- **版本控制**：Git，配合Currsorrules进行代码版本管理
- **本地索引**：Cursor本地索引，用于快速查找相关文件
- **用户登录有效性**：使用Token记录和管理登录时效
- **工作流引擎**：支持通用工作流的复用，提升开发效率

---

## 附加说明

- 系统需支持多公司、多组织架构场景的灵活配置。
- 各模块需预留API接口，方便后续扩展或集成前端需求。
- 需提供详细的接口文档和数据库设计文档，便于开发与后期维护。
