# Poc System 后端API测试使用说明

## 概述

本文档介绍如何使用Postman测试Poc System后端API接口。

## 导入测试集合

1. 打开Postman应用
2. 点击"Import"按钮
3. 选择`PocSystem_Postman_Test_Collection.json`文件
4. 点击"Import"完成导入

## 环境配置

### 设置环境变量

创建新环境，设置以下变量：

| 变量名 | 初始值 | 说明 |
|--------|--------|------|
| `baseUrl` | `http://localhost:8070` | 后端服务基础URL |
| `accessToken` | (空) | 访问令牌 |
| `refreshToken` | (空) | 刷新令牌 |

## 测试流程

### 第一步：启动后端服务

```bash
cd ./backed/backed
./mvnw spring-boot:run
```

### 第二步：执行测试

按顺序执行以下测试：

1. **认证模块** - 先执行登录获取token
2. **用户管理模块** - 测试用户CRUD操作
3. **组织架构模块** - 测试部门管理
4. **工作流模块** - 测试工作流程
5. **费用申请模块** - 测试费用申请流程
6. **代理人设置模块** - 测试AB岗管理
7. **日志管理模块** - 测试日志查询
8. **数据库测试模块** - 测试数据库连接
9. **基础功能模块** - 测试系统基础功能

## 常见问题

### 连接失败
- 确认Spring Boot应用正在运行
- 检查端口8070是否被占用
- 验证`baseUrl`变量设置

### 认证失败
- 先执行登录接口获取token
- 检查token是否过期
- 使用刷新token接口

### 数据库连接失败
- 确认PostgreSQL数据库正在运行
- 检查数据库连接配置

## 注意事项

1. 在测试环境中执行，避免影响生产数据
2. 测试完成后及时清理测试数据
3. 注意不同用户角色的权限差异
4. 关注后端日志输出 