# POC系统 Postman 测试指南

## 概述
本文档提供了使用Postman测试POC系统API的完整指南。测试集合包含了系统所有主要功能模块的API测试用例。

## 文件说明
- `postman_test_collection.json` - Postman测试集合文件
- `POSTMAN_TEST_GUIDE.md` - 本测试指南文档

## 导入测试集合

### 步骤1：导入集合
1. 打开Postman
2. 点击左上角的"Import"按钮
3. 选择"Upload Files"标签
4. 选择`postman_test_collection.json`文件
5. 点击"Import"完成导入

### 步骤2：配置环境变量
导入后，您需要配置以下环境变量：

| 变量名 | 说明 | 默认值 | 备注 |
|--------|------|--------|------|
| baseUrl | 系统基础URL | http://localhost:8080 | 根据实际部署环境修改 |
| authToken | 认证令牌 | 空 | 登录后获取的JWT令牌 |
| userId | 用户ID | 空 | 测试用户ID |
| workflowId | 工作流ID | 空 | 测试工作流ID |
| nodeId | 节点ID | 空 | 测试节点ID |
| expenseId | 费用申请ID | 空 | 测试费用申请ID |
| proxyId | 代理设置ID | 空 | 测试代理设置ID |

## 测试执行顺序

### 1. 基础测试
首先执行基础测试，确保系统正常运行：
- **Hello接口测试** - 验证系统基础响应
- **API测试接口** - 验证API层正常工作

### 2. 用户管理测试
测试用户相关功能：
- **获取所有用户** - 验证用户列表接口
- **根据ID获取用户** - 验证单个用户查询
- **创建用户** - 验证用户创建功能

### 3. 工作流管理测试
测试工作流相关功能：
- **获取所有工作流** - 验证工作流列表
- **获取工作流节点** - 验证节点查询
- **更新节点状态** - 验证状态更新
- **节点打回操作** - 验证打回功能

### 4. 组织架构管理测试
测试组织架构功能：
- **获取组织架构树** - 验证树形结构
- **获取部门列表** - 验证部门查询
- **获取岗位列表** - 验证岗位查询
- **获取在线用户** - 验证在线用户监控

### 5. 费用申请管理测试
测试费用申请功能：
- **获取费用申请单列表** - 验证申请单查询
- **创建费用申请单** - 验证申请单创建
- **获取费用科目** - 验证科目查询
- **获取报销统计** - 验证统计功能

### 6. 代理人设置测试
测试代理功能：
- **获取代理设置列表** - 验证代理设置查询
- **创建代理设置** - 验证代理设置创建
- **获取可代理权限列表** - 验证权限查询

### 7. 日志管理测试
测试日志功能：
- **获取系统日志** - 验证系统日志查询
- **获取安全日志** - 验证安全日志查询
- **获取审计日志** - 验证审计日志查询
- **获取日志统计** - 验证日志统计

## 测试用例说明

### 测试脚本功能
每个测试用例都包含以下验证：
1. **状态码验证** - 确保API返回正确的HTTP状态码
2. **响应格式验证** - 确保响应数据格式正确
3. **数据完整性验证** - 确保返回数据包含必要字段
4. **业务逻辑验证** - 确保业务逻辑正确

### 测试数据管理
- 测试集合使用变量管理测试数据
- 创建操作会自动提取返回的ID到变量中
- 后续测试可以使用这些变量进行关联测试

## 常见问题解决

### 1. 连接失败
**问题**：无法连接到服务器
**解决**：
- 检查`baseUrl`变量是否正确
- 确认后端服务是否启动
- 检查防火墙设置

### 2. 认证失败
**问题**：API返回401未授权
**解决**：
- 检查`authToken`变量是否设置
- 确认JWT令牌是否有效
- 重新登录获取新的令牌

### 3. 测试失败
**问题**：测试用例执行失败
**解决**：
- 检查响应状态码
- 查看响应内容是否符合预期
- 确认测试数据是否正确

### 4. 数据依赖问题
**问题**：测试用例之间存在数据依赖
**解决**：
- 按照测试顺序执行
- 确保前置测试用例成功执行
- 手动设置必要的变量值

## 测试报告生成

### 使用Postman Runner
1. 点击集合右上角的"Run"按钮
2. 选择要运行的测试用例
3. 设置运行参数（迭代次数、延迟等）
4. 点击"Run"开始执行
5. 查看测试结果报告

### 测试结果分析
- **通过率**：成功执行的测试用例比例
- **失败原因**：分析失败的测试用例
- **性能指标**：响应时间、吞吐量等
- **覆盖率**：API接口测试覆盖率

## 持续集成

### Newman命令行工具
可以使用Newman在CI/CD中运行测试：

```bash
# 安装Newman
npm install -g newman

# 运行测试集合
newman run postman_test_collection.json

# 生成HTML报告
newman run postman_test_collection.json -r html
```

### 自动化测试脚本
可以创建自动化脚本定期执行测试：

```bash
#!/bin/bash
# 运行API测试
newman run postman_test_collection.json \
  --environment test_environment.json \
  --reporters cli,html \
  --reporter-html-export test_report.html
```

## 测试环境配置

### 开发环境
- URL: http://localhost:8080
- 数据库: H2内存数据库
- 日志级别: DEBUG

### 测试环境
- URL: http://test-server:8080
- 数据库: MySQL测试库
- 日志级别: INFO

### 生产环境
- URL: https://api.production.com
- 数据库: MySQL生产库
- 日志级别: WARN

## 注意事项

1. **测试数据隔离**：确保测试数据不影响生产数据
2. **并发测试**：避免同时运行多个测试实例
3. **资源清理**：测试完成后清理测试数据
4. **安全考虑**：不要在测试中暴露敏感信息
5. **版本控制**：将测试集合纳入版本控制

## 联系支持

如果在测试过程中遇到问题，请：
1. 查看系统日志文件
2. 检查API文档
3. 联系开发团队
4. 提交问题报告 